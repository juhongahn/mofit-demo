doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="https://use.fontawesome.com/releases/v5.15.4/css/all.css", integrity="sha384-DyZ88mC6Up2uqS4h/KRgHuoeGwBcD4Ng9SiP4dIRy0EXTlnuz47vAwmeGwVChigm", crossorigin="anonymous")
        //- link(rel="stylesheet", href="https://unpkg.com/mvp.css")
        link(rel="stylesheet", href="./public/css/style.css")
        title MoFit
    body 
        header
            i(class="fab fa-pied-piper-square fa-2x")
            h1 MoFit
        main
            div#welcome 
                form
                    input#roomName(type="text", required, placeholder="방 제목")
                    input#nickname(type="text", required, placeholder="이름")
                    button 방 입장
            
            div#call(class="call")
                div#streamBox
                    div#timerBox 
                        p#timer
                    div#streams
                        //div#time
                            //- p#timerBox
                            //-     label(for=timer) 남은 시간 :
                            //-     input#timer(type="text" value="" readonly)
                        div#myStream(class="people1")
                            video#myFace(autoplay, playsinline, width="300", height="800")
                            h3#userNickname  
                            h3#pushCount
                    
                    div#controlers
                        div#controlers__column
                            select#cameras
                        div#controlers__column
                            div#buttons
                                button#mute(class="ableBtn")
                                    i(class="fas fa-microphone fa-2x unMuteIcon")
                                    i(class="fas fa-microphone-slash fa-2x muteIcon")
                                button#camera(class="ableBtn")
                                    i(class="fas fa-video fa-2x cameraIcon")
                                    i(class="fas fa-video-slash fa-2x unCameraIcon")
                                button#readyBtn(class="ready") 준비

                        div#controlers__column
                            button#leave 퇴장
                div#chatRoom
                    ul#chatBox
                    form#chatForm
                        input(required, type="text", placeholder="채팅 입력")
                        button 전송
        

        aside.modal.hidden
            div.modal__box
                h2.modal__text
                button.modal__btn OK



        script(src="https://cdn.jsdelivr.net/npm/sockjs-client@1/dist/sockjs.min.js")
        script(src="/socket.io/socket.io.js")
        script(src="/public/js/app.js")
        script(src="https://cdn.jsdelivr.net/npm/@tensorflow/tfjs@1.3.1/dist/tf.min.js")
        script(src="https://cdn.jsdelivr.net/npm/@teachablemachine/pose@0.8/dist/teachablemachine-pose.min.js")